# Use Node.js base image with Debian for wider Prisma support
FROM node:18-bullseye

# Set working directory
WORKDIR /app

# Copy package files first (better caching)
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy rest of the app
COPY . .

# Ensure prisma engines are built for docker
RUN npx prisma generate

# Expose backend port
EXPOSE 5000

# Start dev server
CMD ["npm", "run", "dev"]
