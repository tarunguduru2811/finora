version: "3.9"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    container_name: finance-backend
    volumes:
      - ./backend:/app
      - /app/node_modules
    ports:
      - "5000:5000"
    environment:
      DATABASE_URL: postgresql://postgres.gaikebsqzivcrechyxac:T2a8r1u1n2002@aws-0-us-east-2.pooler.supabase.com:5432/postgres
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: finance-frontend
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    depends_on:
      - backend
